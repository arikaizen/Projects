name: mcp_server
version: 1.0.0
description: MCP server — Claude API (Anthropic) + Ollama local LLM

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  http: ^1.2.1
  args: ^2.5.0

dev_dependencies:
  lints: ^3.0.0
