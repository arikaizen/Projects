/**
 * @file mcp_types.cpp
 * @brief Compilation unit for mcp_types.h.
 *
 * McpRequest, McpResponse, and ToolCall are plain aggregates with no out-of-line
 * method definitions.  This translation unit exists to confirm that mcp_types.h
 * is self-contained (compiles on its own without extra includes from a .cpp).
 */

#include "mcp_types.h"
